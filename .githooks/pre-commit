#!/usr/bin/env bash
#
# Run the repository pre-commit quality task.
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
readonly REPO_ROOT
cd "${REPO_ROOT}"

exec mise run hook:pre-commit
